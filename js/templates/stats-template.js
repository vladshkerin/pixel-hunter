/** **************************************************************
 *************** Модуль шаблона статистики сайта *****************
 ************************************************************** **/

export default (stats) => {
  return `
    <ul class="stats">
      ${[...stats].map((st) => `
        <li class="stats__result stats__result--${st}"></li>
      `).join(``)};
    </ul>`;
};
